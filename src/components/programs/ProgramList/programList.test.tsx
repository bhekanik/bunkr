import React from 'react';
import { shallow } from 'enzyme';
import ProjectList from '../../projects/ProjectList/ProjectList';

describe('Program List Component', () => {
  it('Should render without errors', () => {
    expect(1).toBe(1);
  });
});
